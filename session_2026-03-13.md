# Session Log: 2026-03-13

## Project Context
- **Project:** managed-rag-api-v1
- **Tool:** Claude Code (Sonnet 4.6)
- **Goal:** Expand file type support beyond PDF/TXT, then run RAGAS baseline eval

## Starting State
- **Branch:** `streamlit-1`
- **Last Working Feature:** RAGAS eval runner built, all bugs fixed, 52 tests passing
- **Known Issues:** Upload endpoint only accepts PDF/TXT — blocks .docx eval docs from being ingested

## Session Progress

### [09:00] — COMPLETE
**Task:** Expand upload file type support to match Google File Search API's full supported format list
**Files changed:**
- `src/api/docs.py` — expanded ALLOWED_TYPES to 10 formats, updated error message
- `tests/test_api.py` — updated error message assertion
- `DATA_CONTRACT.md` — updated accepted MIME types and extensions
- `APP_BRIEF.md` — updated pipeline diagram, validation table, upload page spec, scope locks
**Tests:** 52 passed (6/6 upload tests green)

### [09:15] — COMPLETE
**Task:** Fix Streamlit file uploader blocking .docx uploads at browser level
**Root cause:** Streamlit 1.39.0 does its own MIME type gating before our code runs — passing `"docx"` to `type=[]` is insufficient. Streamlit's internal mapping doesn't handle Office formats correctly.
**Fix:** Set `type=None` on `st.file_uploader` — removes frontend type gate entirely. Backend validation is the real layer and correctly accepts all 10 formats.
**Files changed:**
- `src/streamlit/pages/upload.py` — `type=None` on file uploader, ALLOWED_TYPES expanded to 10 formats, error message updated
**Status:** Tony testing upload of .docx eval docs now

### [resuming] — COMPLETE
**Task:** Update eval/golden_dataset.json — add expected_chunk_keywords to all 28 entries, append Q029-Q033
**Files changed:**
- `eval/golden_dataset.json` — added `expected_chunk_keywords: []` to Q001-Q028, appended Q029-Q033 with populated keywords
**Tests:** 33 questions verified, all have expected_chunk_keywords field, Q031-Q033 correctly populated

### [12:20] — COMPLETE
**Task:** Fix generate_summary failing on .docx — Gemini inline content doesn't support Office MIME types
**Files changed:**
- `requirements.txt` — added python-docx, openpyxl, python-pptx
- `src/services/rag_service.py` — added `_extract_text_for_summary()`, wired into `generate_summary`; added `import io`
**Tests:** 52 passed

### [12:45] — COMPLETE
**Task:** Fix eval/run_eval.py — series of bugs found during live eval run
**Bugs fixed:**
1. `eval/run_eval.py` — added `load_dotenv()` so GEMINI_API_KEY loads from .env automatically
2. `eval/run_eval.py` — added `GoogleGenerativeAIEmbeddings` to evaluate() call (RAGAS was defaulting to OpenAI embeddings)
3. `eval/run_eval.py` — JUDGE_MODEL changed from `gemini-2.0-flash` (deprecated/404) to `gemini-2.5-flash`
4. `eval/run_eval.py` — added `max_tokens=4096` to LLM judge (LLMDidNotFinishException on all 99 jobs → all NaN)
5. `eval/run_eval.py` — pre-flight prompt hardcoded "28 questions" → now uses `len(golden)`
6. `eval/run_eval.py` — summary averages now go through `_score()` to prevent invalid JSON (NaN is not valid JSON)
**Status:** gemini-1.5-flash not available. Switching judge to gemini-2.5-flash-lite. Building test rig first.

### [13:15] — COMPLETE
**Task:** Fix eval pipeline — series of RAGAS + model compatibility issues resolved
**Issues fixed:**
1. `eval/run_eval.py` — JUDGE_MODEL: gemini-2.0-flash (deprecated) → gemini-2.5-flash-lite
2. `eval/run_eval.py` + `eval/test_judge.py` — ragas==0.2.6 LLMDidNotFinishException: Gemini returns "STOP" (uppercase), ragas 0.2.6 only checked "stop" (lowercase). Fixed by pinning ragas==0.2.8 (PR #1727 fix)
3. ragas 0.4.x abandoned — requires InstructorLLM (OpenAI-only), incompatible with LangChain/Gemini
4. Embedding model: text-embedding-004 (404) → gemini-embedding-001 (confirmed working, dim=3072)
5. `eval/test_judge.py` — built model compatibility tester (raw LLM + RAGAS check), NaN=FAIL detection
6. `requirements.txt` — pinned ragas==0.2.8

### [15:30] — COMPLETE
**Task:** RAGAS baseline eval — first successful run
**Results:**
- Questions: 33/33 completed, 0 errors
- Context Recall:    0.3576
- Faithfulness:      0.4536
- Answer Relevancy:  0.8078
- Judge model: gemini-2.5-flash-lite
- Receipts: eval/receipts/eval_2026-03-13.json + .md
**Notes:** Baseline locked. Low recall/faithfulness expected — this is pre-improvement baseline. Q026 answer_relevancy=0.0 flagged for investigation. Negative tests (Q029/Q030) behave as expected.

### [16:00] — IN PROGRESS
**Task:** Replace SUMMARY_PROMPT with structured markdown schema in rag_service.py
